<?xml version="1.0"?>

<project name="Prolint-test" default="">

  <taskdef resource="PCT.properties" classpath="." />

  <property environment="env" />

  <!-- Should throw BuildException : no project file -->
  <target name="test1">
    <Prolint dlcHome="${DLC}" />
  </target>

  <!-- Simple test : one successful test case, no result file, no template -->
  <target name="test2">
    <mkdir dir="lint" />
    <echo file="sandbox/msg.p">
      message "foo" view-as alert-box.
    </echo>
    <Prolint dlcHome="${DLC}" destDir="lint">
      <fileset dir="sandbox" includes="*.p" />
      <lintHandler name="xml.p" />
    </Prolint>
  </target>

</project>