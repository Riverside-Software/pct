<?xml version="1.0"?>

<project name="PCTCRC-test" default="">

  <taskdef resource="PCT.properties" classpath="."/>

  <property environment="env"/>
  
  <!-- Should throw BuildException : no dest file -->
  <target name="test1">
    <PCTCRC dlcHome="${env.DLC}"/>
  </target>
  
  <!-- No database connection : should throw BuildException -->
  <target name="test2">
    <PCTCRC destFile="sandbox/crc" dlcHome="${env.DLC}" propath="tty"/>
  </target>
  <target name="test2post">
    <delete file="sandbox/crc"/>
  </target>
  
  <!-- One database -->
  <target name="test3pre">
    <echo file="sandbox/schema.df">
      UPDATE DATABASE "?"
      
      ADD TABLE "Tab1"
        AREA "Schema Area"
        LABEL ""
        DESCRIPTION ""
        DUMP-NAME "Tab1"

      ADD FIELD "Fld1" OF "tab1" AS integer 
        FORMAT "9"
        INITIAL "0"
        LABEL "Fld1"
        POSITION 2
        SQL-WIDTH 4
        COLUMN-LABEL "Fld1"
        ORDER 10

      ADD INDEX "Tab1-PK" ON "tab1" 
        AREA "Schema Area"
        UNIQUE
        PRIMARY
        DESCRIPTION "Primary key"
        INDEX-FIELD "Fld1" ASCENDING 
    </echo>
    <PCTCreateBase dbName="test" destDir="sandbox" dlcHome="${env.DLC}" schemaFile="sandbox/schema.df" propath="tty"/>
  </target>
  <target name="test3">
    <PCTCRC destFile="sandbox/crc" dlcHome="${env.DLC}" propath="tty">
      <PCTConnection dbName="test" dbDir="sandbox" singleUser="true"/>
    </PCTCRC>
  </target>

  <!-- One database -->
  <target name="test4pre">
    <echo file="sandbox/schema.df">
      UPDATE DATABASE "?"
      
      ADD TABLE "Tab1"
        AREA "Schema Area"
        LABEL ""
        DESCRIPTION ""
        DUMP-NAME "Tab1"

      ADD FIELD "Fld1" OF "tab1" AS integer 
        FORMAT "9"
        INITIAL "0"
        LABEL "Fld1"
        POSITION 2
        SQL-WIDTH 4
        COLUMN-LABEL "Fld1"
        ORDER 10

      ADD INDEX "Tab1-PK" ON "tab1" 
        AREA "Schema Area"
        UNIQUE
        PRIMARY
        DESCRIPTION "Primary key"
        INDEX-FIELD "Fld1" ASCENDING 
    </echo>
    <PCTCreateBase dbName="test" destDir="sandbox" dlcHome="${env.DLC}" schemaFile="sandbox/schema.df" propath="tty"/>
    <PCTCreateBase dbName="test2" destDir="sandbox" dlcHome="${env.DLC}" schemaFile="sandbox/schema.df" propath="tty"/>
  </target>
  <target name="test4">
    <PCTCRC destFile="sandbox/crc" dlcHome="${env.DLC}" propath="tty">
      <PCTConnection dbName="test" dbDir="sandbox" singleUser="true"/>
      <PCTConnection dbName="test2" dbDir="sandbox" singleUser="true"/>
    </PCTCRC>
  </target>
</project>
