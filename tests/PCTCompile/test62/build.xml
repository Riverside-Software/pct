<?xml version="1.0"?>
<project name="PCTCompile-test62">
  <taskdef resource="PCT.properties" />

  <target name="test">
    <mkdir dir="build" />
    <PCTCompile destDir="build" dlcHome="${DLC}">
      <fileset dir="src" includes="*.p" />
      <Option name="-swl" value="swl.txt" />
      <propath location="src" />
      <Profiler enabled="${PROFILER}" coverage="true" outputDir="profiler" />
    </PCTCompile>
  </target>

</project>
