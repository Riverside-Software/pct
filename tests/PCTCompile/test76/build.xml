<?xml version="1.0"?>
<project name="PCTCompile-test76">
  <taskdef resource="PCT.properties" />

  <target name="test">
    <mkdir dir="build" />
    <PCTCompile graphicalMode="false" destDir="build" dlcHome="${DLC}" callbackClass="CompileCallback">
      <fileset dir="src" includes="test.p" />
      <Profiler enabled="${PROFILER}" coverage="true" outputDir="profiler" />
    </PCTCompile>
  </target>

</project>
